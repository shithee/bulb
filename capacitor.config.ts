import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cricketist.bulb',
  appName: 'bulb',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
